{
  "name": "lef-calendar-scraper",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Captures the LEF calendar plan allowlist from the Redpoint HQ storefront calendar SPA.",
  "scripts": {
    "capture": "node capture-calendar-input.mjs",
    "dev": "node dev-server.mjs"
  },
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
